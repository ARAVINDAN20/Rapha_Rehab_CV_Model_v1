# PhysioGuard - Server Dependencies (Client-Side Architecture)
# No MediaPipe or OpenCV needed on the server!
# All AI inference runs in the user's browser.

flask>=3.0.0
gunicorn>=21.2.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
